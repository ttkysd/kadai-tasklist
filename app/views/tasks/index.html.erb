<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |tasks| %>
  
  <li>
    <%= tasks.content %> - <%= link_to "詳細", tasks %>
    【<%= tasks.status %>】
  </li>
  
  <%end%>
  
</ul>

<%= link_to "新規タスクの登録", new_task_path %>